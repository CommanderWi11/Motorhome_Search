// Copy this file to config.js and fill in your Supabase credentials.
// config.js is NOT gitignored and must stay committed — GitHub Pages (legacy mode)
// serves static files straight from main with no server-side secret injection.
// The anon key is meant to be public client-side; RLS policies control access.
const SUPABASE_URL = "https://YOUR_PROJECT.supabase.co";
const SUPABASE_ANON_KEY = "YOUR_ANON_KEY";
